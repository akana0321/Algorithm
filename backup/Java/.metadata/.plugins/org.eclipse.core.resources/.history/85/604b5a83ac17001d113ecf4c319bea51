package dovelet.step3;

import java.io.*;

public class Hangover {
	public static void main(String[] args) throws Exception {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		double c = Double.parseDouble(br.readLine());
		int card = 2;
		double accumulate = 0.00;
		
		while(true) {
			accumulate += (double)Math.round((double)1/card*100)/100;
			System.out.println("더하는거: " + (double)Math.round((double)1/card*100)/100);
			System.out.println("accumulate: " + accumulate);
			if(accumulate >= c) {
				System.out.println(card-1 + " card(s)");
				break;
			}
			card++;
		}
	}
}
